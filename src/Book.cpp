#include "book.h"
/*
Book::Book(QObject *parent, QString title, QString auther, QString category, QString barcode) : QObject{parent}, _title(title)
    , _auther(auther), _category(category), _barcode(barcode)
{}
*/


Book::Book(QObject *parent): QObject{parent}
{}
